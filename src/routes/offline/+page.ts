// Precached by the service worker as the offline fallback for navigation requests it can't
// serve any other way, so it must exist as static HTML in the build output.
export const prerender = true;
